var
i,j,k,n,m:longint;
st,tt:ansistring;
b:array[1..500] of boolean;
function tr(l,r):boolean;
var
i,j,k:longint;
begin

end;
begin
assign(input,'makeitmoreharmonious.in');reset(input);
assign(output,'makeitmoreharmonious.out');rewrite(output);
 readln(st);
 tt:='+('+st+')';
 n:=length(st);
 tr(2,n);
close(input);
close(output);
end.
