var
i,j,k,n,m,l:longint;
z:array[1..100] of longint;
st:string;
begin
 assign(input,'palin.in');reset(input);
 assign(output,'palin.out');rewrite(output);
  writeln(1);
 close(input);
 close(output);
end.
